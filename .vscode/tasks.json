{
  "type": "shell",
  "label": "Run",
  "command": "cmd.exe",
  "args": [
    "/c",
    "\"${fileDirname}\\${fileBasenameNoExtension}.exe & pause\""  // Exécuter le fichier .exe et ajouter une pause pour voir le résultat
  ],
  "options": {
    "shell": {
      "executable": "cmd.exe"
    }
  },
  "dependsOn": "C/C++: g++.exe build active file",  // Assure que le build est effectué avant le run
  "group": "test",
  "problemMatcher": []
}
